#!/bin/bash
# Report whether OpenSSL FIPS mode is active per image openssl config (not host kernel FIPS leak).
# See docs/building-on-fips-enabled-hosts.md

set -e

if ! command -v openssl &>/dev/null; then
  echo "openssl could not be found"
  exit 1
fi

fips_openssl_config() {
  if [[ -n "${OPENSSL_CONF:-}" ]]; then
    echo "${OPENSSL_CONF}"
    return
  fi
  echo "$(openssl version -d | sed -n 's/.*"\(.*\)"/\1/p')/openssl.cnf"
}

fips_enabled() {
  local conf
  conf=$(fips_openssl_config)
  [[ -f "${conf}" ]] && grep -qE 'default_properties\s*=\s*"?fips=yes' "${conf}"
}

print_fips_state() {
  if fips_enabled; then
    echo "FIPS mode is enabled"
  else
    echo "FIPS mode is disabled"
  fi
}

print_fips_state

set +e
